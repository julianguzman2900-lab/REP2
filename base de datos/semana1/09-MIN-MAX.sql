-- Obtenr el valor menor del campo de edad de la tabla 
Select MIN(age) AS min_age
FROM users
-- obtenr el valor maximo de edad de la tabla 

SELECT MAX(age) AS max_age, MIN(age) AS min_age
FROM users 







