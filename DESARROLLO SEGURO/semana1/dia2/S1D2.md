## Inyecciones sql
- palabras reservadas: SELECT, FROM, WHERE, AND
- identificadores: nombres de las tablas o nombres de las columnas
- operadoresL =, >, LIKE
- literales: cadenas de texto en delimitadores: 'diego', '1234'

2. análisis sintactico (El parser)
- verifica que cumpla las reglas gramaticales del lenguaje SQL
- si la sintaxiss es correcta
- construye una estructura matematica llamada AST ( ABSTRACT SYNTAX TREE) o árbol de sintaxis abstracta
- mapa que el motor va a usar para qsaber qué buscar y qué responder

3. optimizador de consultas
calcula el camino más eficiente para recuperar los datos

4. El motor de ejecución
- transforma el AST optimizado en instrucciones binarios de bajo nivel


la inyección no es un truco de magia, es una vulnerabilidad de confusión de contexto. transforma un literal de datos en una instrucción de control (codigo ejecutable)

### Ejemplo 1
```js
const query = `SELECT * FROM users WHERE user = '${username}' AND pass = '${password}'`;
```
si el usuario ingresa un dato limpio en el formulario de login:
* username => 'diego'
* password => 'pass123'

```text
       [Query]
       /     \
   [SELECT]   [WHERE]
      |       /     \
   (users)  [AND]   [AND]
            /           \
     (user='diego')   (pass='pass123')
```

### Ejemplo 2: consulta no legítima

* username => 'admin' --
* password => (vacío)

```sql
SELECT * FROM users WHERE user = 'admin' -- AND pass = '${password}';
```

```text
       [Query]
       /     \
   [SELECT]   [WHERE]

      |          |
   (users)   (user='admin')
             // [Todo lo posterior es ignorado como comentario]
```


#### por qué funciona
- la conmilla inyectada '
- comentar el resto del código --

## Carácteries criticos
- `'`: la comilla simple. cierre de delimitador de cadena
- `--` o `#**`. Comentarios
- `OR` / `AND`. Alterar las tablas de verdad
- `;`: delimitador de fin de sentencia


SQLi

## Manipulación de datos: ` ' ; DROP TABLE users;-- `

consulta resultante: SELECT * FROM users WHERE username = ' ' ; DROP TABLE users;--

## inyection clásica
` ' O '1'='1' `

la base de datos podría ejecutar lo siguiente:
` select * from productos where nombre = ' ' O '1'='1' `;

## inyección a ciegas

- basada en booleano:
- ' AND 1=1--
' AND 1=2--

## basada en errores
## basada en uniones
## en segundo orden

`Diego; DROP TABLE users`

# prevenir
SELECT * FROM users WHERE username = '"' + userInput + '"' AND password = '"' + userPassword + '"';

consulta = "SELECT * FROM users WHERE username = ? AND password = ?"
cursor.execute(query, (userInput, userPassword))
